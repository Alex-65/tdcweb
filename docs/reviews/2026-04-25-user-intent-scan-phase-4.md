# User Intent Scan — Phase 4 (2026-04-25, opening)

**Reviewer:** controller (main Claude)
**Trigger:** CLAUDE.md rule 19 / rule 16 step (a0)
**Scope:** conversation window since Phase 3 close (`4ab1094`) through Phase 4 kickoff
**Purpose:** capture user decisions / requests / scope changes that must be codified in the plan or deferred, BEFORE any phase-end review runs.

Baseline: `docs/reviews/2026-04-24-user-intent-scan-phase-3.md` (rows 1-46 closed).

---

## Scan table — user decisions vs codification (opening)

| # | User decision / request | Where codified | Status |
|---|---|---|---|
| 1 | Phase 4 scope = Tasks 4.1 through 4.4 per `docs/superpowers/plans/2026-04-23-nuxt-integration.md` §Phase 4 (login TDD, logout/refresh/me, revalidate, useAuth + middleware) | plan §Phase 4 | ✅ codified |
| 2 | Production web server is Apache (mod_proxy_http), NOT nginx -- correction landed in commit `4ab1094` for top-level docs (CLAUDE.md, README, pdp-v3); spec/plan/playbook deeper references deferred to Phase 6 dispatch when actual Apache vhost lands | top-level docs corrected; memory `~/.claude/projects/-data1-tdcweb-dev/memory/project_apache_not_nginx.md` | ✅ codified -- Phase 6 will sweep remaining mentions |
| 3 | Dev server (`localhost:9503`) currently NOT internet-exposed: no nginx/Apache vhost proxy to dev ports, no firewall opening, only LAN/loopback. Decision deferred whether to set up Apache dev vhost (`dev.thedreamerscave.club` -> :9503 with HTTP basic auth + IP allowlist) before Phase 4 finishes for browser-based login flow testing. | Pending user decision during Phase 4 (likely after Task 4.1 lands and we want to test login round-trip from real browser) | ⏳ pending user decision |
| 4 | TDD discipline scope for Phase 4: plan prescribes TDD only for Task 4.1 (login.post.ts). Tasks 4.2 (logout/refresh/me), 4.3 (revalidate), 4.4 (useAuth+middleware) do NOT have unit-test steps in the plan. Phase 3 lesson C.6: TDD pattern replicable; should we elevate to all handlers? | Decision: stick with plan (TDD only for 4.1). Other handlers exercised by Playwright E2E during Phase 7 DoD smoke + by the Phase 4 atomic-commit real-tests step (b). Rationale: 4.2 handlers are thin pass-throughs (cookie set + flaskFetch); 4.3 has a single role-gate + cache-clear; 4.4 is client-side and needs @nuxt/test-utils which is out of Phase 4 scope. | ✅ codified -- TDD only on 4.1 |
| 5 | Test mock pattern for server-route handlers: Phase 3 Task 3.8 (useApi) codified rule against `as any` in test mocks (Gap #1, scan row 3). Plan Task 4.1 Step 1 contains 6 occurrences of `as any` in globalThis mock setup -- predates the lesson. | Implementer brief for Task 4.1 will explicitly require typed pattern: `(globalThis as unknown as { name: Type }).name = mock`. Plan §Task 4.1 Step 1 will be amended in phase-end docs-sync to reflect the typed shape (same precedent as Gap #1 closure for Task 3.8 plan amendment row 3). | ✅ codified -- typed-mock requirement carried forward |
| 6 | Auto-import of server utils (`flaskFetch`, `setAccessCookie`, `setRefreshCookie`, `getRefreshToken`, `clearAuthCookies`) in Nitro route handlers is a Phase 2 invariant. Plan Task 4.1-4.3 handler snippets assume bare-identifier usage (no explicit imports). | If at first dispatch the implementer finds the auto-imports don't resolve, fall back to explicit imports from `~/server/utils/{flask-client, cookies}.ts`. Probably fine -- Phase 2 didn't test server-util auto-imports because no consumer existed; Task 4.1 is the first real consumer. | ⏳ verify on first dispatch |
| 7 | Phase 4 first integration test of `flaskFetch` resilient pattern (Task 3.0 / TD-009 closed). Phase 3 SSR bundle audit confirmed flask-client.ts tree-shaken because no consumer; Task 4.1 will be the first consumer that pulls it into the prod bundle. Real prod-build test at phase end will exercise the resilient pattern in actual Nitro context. | TD-009 close-when criterion (b): "invoked from a real server route in prod build mode without error" gets satisfied during Phase 4 real-tests. Logged for the phase-end review. | ✅ codified |
| 8 | Phase-end governance: rule 16 protocol applied identically to Phase 3 (closing scan, holistic code + design review, real tests, state cleanup, docs sync, atomic commit). Push only with explicit user authorization. | `CLAUDE.md rule 16`, this scan's closing section (to be filled at phase end) | ✅ codified |
| 9 | Per-task checkpoint discipline maintained: implementer report + code-reviewer + design-system-enforcer (even on non-UI tasks where it returns `[N/A]` per phase-scoped elevation row 30 from Phase 3) + STOP for user OK. User confirmed mid-Phase-3 that this discipline stays in force across phases. | Phase 3 row 30 + this scan | ✅ codified |
| 10 | TDC v9+ i18n module + reduced-motion patterns + `--color-error` token + `useApi` retry pattern + preemptive-polish pattern from Phase 3 are all available substrate for Phase 4 consumers. `useAuth` (Task 4.4) will be the first non-trivial consumer of `useApi` semantics in production. | Phase 3 lessons C.1-C.7 codified in scan + playbook; Phase 4 implementer briefs reference them. | ✅ codified -- substrate-ready |
| 11 | Apache dev vhost SETUP completed mid-Phase-4 (post-Task 4.1, pre-Task 4.3) per user decision 2026-04-25 ("1 - si"). `dev.thedreamerscave.club` resolves to mioh1 public IP (46.4.40.209). Let's Encrypt SEPARATE cert (NOT consolidated with `unified-cert` -- error of process: should have inventoried first; user accepted leaving as separate cert per "lasciamolo separato per ora"). HTTP basic auth `tdctest` w/ bcrypt hash in `/etc/apache2/.htpasswd-tdc-dev`. Apache vhost `/etc/apache2/sites-available/dev-thedreamerscave.conf` with HTTP->HTTPS redirect + ProxyPass to 127.0.0.1:9503 (Option Y -- Nuxt internal routing handles /api/auth, /api/revalidate, devProxy to Flask :9502 for other /api). `/etc/hosts` adds `127.0.0.1 dev.thedreamerscave.club` for loopback testing of the cert. dev.sh starts both Flask :9502 and Nuxt :9503. | `/etc/apache2/sites-available/dev-thedreamerscave.conf`, `/etc/apache2/.htpasswd-tdc-dev` (bcrypt, 0640 root:www-data), `/etc/hosts`, `/etc/letsencrypt/live/dev.thedreamerscave.club/`, /var/www/dev-acme/.well-known/acme-challenge/. Memory `~/.claude/projects/-data1-tdcweb-dev/memory/project_apache_not_nginx.md` already had Apache directive. | ✅ codified -- operational |
| 12 | Backend test coverage scope expansion: user request 2026-04-25 -- "creasse una test suite completa per il backend che usa pytest". Recon discovered Flask backend is at MVP-stub stage: only `/api/health/*` endpoints exist, NO auth/locations/events endpoints, models directory empty, `tests/` directory empty. DB schema HOWEVER is fully populated in `tdcweb_dev` (35 tables incl. users, oauth_accounts, locations, events, *_translations) -- schema applied directly without migration tracking. Phase 4 frontend BFF (Tasks 4.1-4.4) was assuming Flask endpoints that don't exist. User chose option D' 2026-04-25: pause Phase 4 frontend (Tasks 4.3 + 4.4 deferred), inject **Phase 4B parallel** to build Flask auth blueprint + locations + events read endpoints + complete pytest suite via real MySQL fixtures (CLAUDE.md no-DB-mock rule). Resume Phase 4 frontend after 4B closes. | Phase 4B subtasks: 4B.1 (User model + JWT + bcrypt + auth decorators + conftest base), 4B.2 (auth blueprint + test_auth.py ~12 cases), 4B.3 (locations + events read blueprints + tests). Out of 4B scope: OAuth, email verification, registration endpoint (seed via pytest fixture), password reset, admin/staff write endpoints (defer to later phases). Test DB: new `tdcweb_test` schema-replicated from tdcweb_dev, conftest manages session lifecycle. | ✅ codified -- 4B injected, 4.3/4.4 deferred |
| 13 | Backend test coverage Phase 4B observation: Phase 4 frontend BFF Tasks 4.1 + 4.2 already shipped (uncommitted) with handlers that proxy to Flask `/api/auth/*` endpoints which don't exist yet. After 4B.2 lands real Flask auth, an integration smoke test (curl http://localhost:9502/api/auth/login + Nuxt BFF round-trip) becomes possible. Frontend handler tests already pass at vitest level via globalThis stubs; Phase 4 phase-end real tests will exercise the full BFF -> Flask -> DB path once 4B is in. | Add to phase-end real-tests step: `curl -X POST http://localhost:9502/api/auth/login -d '{...}'` directly + via BFF proxy `https://dev.thedreamerscave.club/api/auth/login` with basic auth. | ✅ codified -- integration smoke at phase end |
| 14 | Pre-flight controller alignment 2026-04-25 (before 4B.2 dispatch): `app/routes/api/__init__.py` `api_bp.url_prefix` dropped from `/api/v1` to `/api`. The /v1 prefix was scaffolding leftover that contradicted spec/plan/CLAUDE.md/pdp-v3 (all reference /api/auth/**, /api/revalidate, /api/** without versioning) AND the frontend BFF expectation (Tasks 4.1-4.2 hit /api/auth/*). Health endpoints moved from /api/v1/health to /api/health. | `backend/app/routes/api/__init__.py` line 8 (prefix) + lines 1-13 (docstring rationale). Pytest 5/5 foundation still passes after change. README + CHANGELOG + docs/api/health.md + docs/deployment/development.md all reference /api/v1/health and need amendment at phase-end docs-sync. | ✅ codified -- pre-flight applied |
| 15 | Critical contract bug discovered post-4B.2 implementation by holistic code-reviewer: backend Flask `auth.py` correctly uses `responses.success(data)` envelope `{success: true, data: {...}}` per project convention (`responses.py` docstring "All endpoints MUST use these"), but frontend BFF Tasks 4.1-4.2 (already shipped uncommitted) read flat shape (`response.access`, `response.user`). Plan inline code prescribed flat -- was OUT OF SYNC with responses.py from Phase 2 inception. Independently both backend pytest 18/18 + frontend vitest 23/23 passed because no integration test crossed the BFF<->Flask boundary. Browser login round-trip would silently break (cookies set to undefined). User chose option (B) 2026-04-25: BFF unwraps `.data`. | Controller micro-edits applied 2026-04-25 to 5 files: `frontend/app/types/api.ts` (added `success: true` to ApiResponse, added ApiErrorResponse), `frontend/server/api/auth/{login,refresh,me}` (typed flaskFetch as `ApiResponse<T>`, read `.data.access` etc.), `frontend/tests/unit/login-handler.test.ts` (mock returns enveloped shape). Verified: nuxi typecheck 0 errors, vitest 23/23, pytest 23/23 (no backend changes). Plan inline code amendment for Tasks 4.1-4.2 + 4B.2 deferred to phase-end docs-sync. | ✅ codified -- envelope canonical, BFF unwraps |
| 16 | Phase 3 read pages latent bug discovered during scan row 15 investigation: `frontend/app/pages/{locations,events}/index.vue` use `useFetch<Location[]>('/api/locations')` and `useFetch<Event[]>('/api/events')` -- direct-to-Flask via Apache devProxy, no BFF intermediation. When 4B.3 lands real Flask read endpoints, those pages would receive `{success: true, data: [...]}` envelope but template iterates `locations.value` as if it were the array -- `v-for="location in locations"` over the envelope object, undefined behavior. | Decision deferred to Task 4B.3 dispatch: choose envelope strategy for read endpoints. Candidates: (i) shared adapter in useApi/composable, (ii) per-page unwrap in useFetch transform option, (iii) backend skip envelope for read endpoints. Pending. | ⏳ pending Task 4B.3 dispatch decision |

---

## Items deferred (with explicit rationale)

- **Apache dev vhost (row 3, now row 11)**: SETUP DONE mid-Phase-4. (Resolved.)
- **Plan amendment for Task 4.1 typed mocks (row 5)**: defer to phase-end docs-sync (same precedent as Gap #1 plan amendment for Task 3.8 done at Phase 3 docs-sync). Implementer applies typed pattern at code level immediately via brief.
- **Server-util auto-import verification (row 6)**: verified at first dispatch, fixed inline if needed.

---

## Closing scan (Phase 4 end, before triple review) -- 2026-04-25

| # | User decision / request | Where codified | Status |
|---|---|---|---|
| 17 | Envelope adapter strategy (resolves row 16): user chose option (i) "shared adapter in useApi/composable" 2026-04-25. Implemented as `useApiFetch<T>` composable in Task 4B.4 wrapping `useFetch` with envelope-aware `transform: unwrapEnvelope<T>`. Read pages `/locations` and `/events` retrofitted via 1-line swap. | `frontend/app/composables/useApiFetch.ts` (new), `frontend/tests/unit/useApiFetch.test.ts` (new, 12 cases), pages updated. | ✅ codified |
| 18 | devProxy target stripping bug (discovered Task 4B.4 integration testing): Nitro strips matched key (`/api`) before forwarding to target. Original `target: 'http://localhost:9502'` caused requests to forward to `/locations` (404 on Flask). User implicitly approved fix via "ok, vai" 4B.4 dispatch. | `frontend/nuxt.config.ts` line 63: target now `(NUXT_FLASK_URL or http://localhost:9502) + '/api'`. Inline comment documents the gotcha. | ✅ codified |
| 19 | SSR fetch baseURL fix (discovered post-devProxy fix, Task 4B.4): Vite devProxy is a dev-server middleware that does NOT intercept Nitro's internal SSR `$fetch`. Relative `/api/...` URLs during SSR fall through to the Vue page renderer; Vue Router emits "Page not found: /api/locations". Fix: `useApiFetch` sets `baseURL = useRuntimeConfig().flaskUrl` on `import.meta.server`, undefined on client. User implicitly approved by accepting Task 4B.4 fix round + approving review re-pass. | `frontend/app/composables/useApiFetch.ts:60-90` with extracted `resolveApiBaseURL(isServer, flaskUrl)` pure helper (3 vitest cases). Doc comment encodes the "two debug cycles" gotcha. | ✅ codified |
| 20 | "Tutto fix now" preemptive-polish pattern (Phase 3 carry-over) explicitly reconfirmed during Task 4B.4 review round: code-reviewer found 1 HIGH (loose envelope guard) + 2 MEDIUM (no SSR baseURL test, missing flaskUrl assertion). User implicitly approved fix-all-now by not asking to defer when prior tasks established the pattern. All three fixed in same dispatch round; re-review APPROVED no new findings. | Strict guard + `resolveApiBaseURL` extraction landed in `useApiFetch.ts`; 6 new vitest cases lock the regressions. | ✅ codified |
| 21 | Cache key research (Task 4.3 implementation): both the spec sec 7.3 (`nitro:routes:` prefix) and the plan §Task 4.3 (`routes:` prefix) had the WRONG Nitro storage key format. Implementer read `nitropack/dist/runtime/internal/cache.mjs:29,124-145` + `app.mjs:131` and derived the correct format `cache:nitro:routes:_:<escapedPathname>.<hash>.json`. Reviewer verified citations source-by-source. User implicitly approved by proceeding to Task 4.4. | Handler at `frontend/server/api/revalidate.post.ts` with prefix-list approach robust to future Nitro key-format changes. Spec sec 7.3 + plan Task 4.3 inline code remain wrong -- to be reconciled by `tdc-documentation-expert` at step (d). | ⏳ docs-sync at step (d) -- spec sec 7.3 + plan Task 4.3 cache-key text |
| 22 | TECH_DEBT TD-014 logged 2026-04-25 (`NUXT_FLASK_URL` deployment runbook gap): Task 4B.4 introduced a defensive throw in `resolveApiBaseURL` if `flaskUrl` is empty on SSR. The OPERATIONAL requirement (systemd unit / Apache vhost env var) is not yet documented. Defer to Phase 6 deployment runbook rather than create stub doc now. User explicit instruction: "marca il 'cosa potrebbe rompere' nel TECH_DEBT". | `docs/TECH_DEBT.md` TD-014. Resolution trigger: Phase 6 systemd unit definition. | ✅ codified |
| 23 | `useRequestFetch()` SSR cookie-forwarding pattern (Task 4.4 deviation from plan): plan inline code uses raw `$fetch('/api/auth/me', {credentials: 'include'})` for `fetchMe`. During SSR, raw `$fetch` does NOT forward incoming-request cookies to internal Nitro routes -- chain returns 401 SSR-side and bounce-loops authenticated users to login. Controller flagged preemptively in implementer brief; implementer applied `useRequestFetch()` (returns $fetch instance pre-bound to H3Event). User implicitly approved via "ok, prosegui". | `frontend/app/composables/useAuth.ts:120-140` with doc-comment explaining the SSR cookie-forwarding contract. Plan §Task 4.4 inline code (line 2480, 2490, 2500) still shows raw `$fetch` -- to be reconciled at step (d). | ⏳ docs-sync at step (d) -- plan Task 4.4 inline code |
| 24 | Pure-helper extraction architectural pattern (Task 4B.4 + 4.4): both tasks extracted decision logic into pure functions exported alongside the composable/middleware: `unwrapEnvelope` + `resolveApiBaseURL` (Task 4B.4), `buildAuthOps` + `buildLoginRedirect` + `decideAuthOutcome` + `decideAdminOutcome` + `decideStaffOutcome` (Task 4.4). Test the pure functions exhaustively; thin wrapper over Nuxt auto-imports gets exercised by phase-end E2E. Pattern is now established TDC convention for app-code testability. User implicitly approved by accepting both review rounds. | Documented inline in each file's header comment. To be elevated to `docs/frontend/nuxt-playbook.md` testing section by `tdc-documentation-expert` at step (d). | ⏳ docs-sync at step (d) -- playbook testing section |
| 25 | New directory `app/utils/` (Task 4.4): houses pure non-reactive helpers (`auth-guard.ts`). Nuxt 4 auto-imports from `app/utils/` since v3.4. Code-reviewer noted to update project-structure doc. Acceptable as TDC convention. | Files exist in tree; project-structure mention in CLAUDE.md / pdp-v3 / playbook to be added by `tdc-documentation-expert` at step (d). | ⏳ docs-sync at step (d) |
| 26 | TECH_DEBT TD-015 logged 2026-04-25 (login-page open-redirect validation): three guards bounce guests to `/auth/login?redirect=<encoded>`. Login page (future task) MUST validate the param to prevent open-redirect via `?redirect=https://evil.example/`. User explicit instruction at Task 4.4 close: "marca il 'cosa potrebbe rompere' nel TECH_DEBT". | `docs/TECH_DEBT.md` TD-015. Resolution trigger: first task creating `app/pages/auth/login.vue`. | ✅ codified |
| 27 | Phase 4 end-of-phase execution mode (2026-04-25): user authorized full sequential execution of rule 16 protocol -- "procedi con tutto in sequenza, fermati solo se ci sono problemi". Departure from per-task stop-and-wait discipline (rule 15 still in force at task level; phase-end is a single coordinated sweep). | This scan + this row. Codified for Phase 4 only -- per-task discipline resumes for Phase 5. | ✅ codified |
| 28 | Pytest test count for Phase 4B (verification): user did not request a specific count but expects "complete pytest suite". As-built: foundation (5) + auth (18) + locations (10) + events (12) = 45 cases. Real-MySQL fixtures (no mocks) per CLAUDE.md no-DB-mock rule. State cleanup verified at conftest fixture level. | `backend/tests/test_{foundation,auth,locations,events}.py`. To be re-run at step (b) to confirm green + DB cleanup. | ✅ codified |

---

## Reconciliation tasks for `tdc-documentation-expert` (step (d))

The following docs drift was identified during the scan and MUST be addressed by the documentation expert during the phase-end docs-sync:

1. **Spec `2026-04-23-nuxt-integration-design.md` sec 7.3**: cache key uses `nitro:routes:${path}.json` -- WRONG. As-built handler uses `cache:nitro:routes:_:<escapedPathname>.<hash>.json` per Nitro source. Update spec inline code to match the as-built handler with citation to the Nitro source.
2. **Plan `2026-04-23-nuxt-integration.md` Task 4.3 (line 2444)**: cache key uses `routes:${path}.json` -- WRONG. Same correction as #1.
3. **Plan §Task 4.4 (lines 2480, 2490, 2500)**: inline code uses raw `$fetch` with `credentials: 'include'`. As-built uses `useRequestFetch()` for SSR cookie forwarding. Update inline code with rationale comment.
4. **Plan §Task 4.1 Step 1**: inline code uses 6 occurrences of `as any` in globalThis mock setup -- predates Phase 3 Gap #1 typed-pattern lesson. Update to typed pattern `(globalThis as unknown as { name: Type }).name = mock`.
5. **Plan §Task 4.1-4.4 Step "Commit"**: explicit `git commit` instruction violates rule 15 (no per-task commits). Replace with reference to phase-end commit at rule 16(e).
6. **Backend `/api/v1` -> `/api` prefix change** (row 14): README + CHANGELOG + `docs/api/health.md` + `docs/deployment/development.md` reference `/api/v1/health` -- update to `/api/health`.
7. **Project-structure docs** (CLAUDE.md / pdp-v3 / playbook) mention `app/composables/`, `app/types/`, `app/components/` etc. -- add `app/utils/` (Task 4.4) to the inventory.
8. **Frontend playbook testing section**: codify the pure-helper extraction pattern (`unwrapEnvelope`, `resolveApiBaseURL`, `buildAuthOps`, `decide*Outcome`) as the canonical TDC pattern for testing composables/middleware that depend on Nuxt auto-imports.
9. **CLAUDE.md "🤖 INTELLIGENT AGENT SYSTEM"**: agent count "(27 Agents)" predates the review-guardian additions -- verify count is still accurate after phase 4.
10. **CHANGELOG.md**: append Phase 4 entry with all task summaries.
